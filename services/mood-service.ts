import { supabase } from '@/lib/supabase/client';

export const moodService = {
  async getMoods(spaceId: string) {
    // Stub
    return [];
  },
  async createMood(spaceId: string, mood: string, note?: string) {
    // Stub
    return null;
  }
};
