import { supabase } from '@/lib/supabase/client';

export const journalService = {
  async getEntries(spaceId: string) {
    // Stub
    return [];
  },
  async createEntry(spaceId: string, title: string, content: string) {
    // Stub
    return null;
  }
};
